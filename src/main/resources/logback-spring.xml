<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <!-- 输出到控制台 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOGIN_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/login/login.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/login/login.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>



    <logger name="com.lld.auth.utils.LoginLogUtil" level="INFO" additivity="false">
        <appender-ref ref="LOGIN_LOG_FILE"/>
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- 定义日志级别和输出目的地 -->
    <root level="info">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>


